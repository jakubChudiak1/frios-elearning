/* const links = document.getElementsByTagName("a");
console.log("runnning");
Array.from(links).forEach((link) => {
  link.addEventListener("beforeunload", function () {
    window.scrollTo(0, 0);
  });
});
 */
