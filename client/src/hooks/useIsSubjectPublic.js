import { useState } from "react";
import useFetchData from "./useFetchData";
import apiConfig from "../config/api.config";

const useIsSubjectPublic = (subjectId) => {};
export default useIsSubjectPublic;
